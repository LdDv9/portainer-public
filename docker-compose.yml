version: "1.0"
services:
  web:
    restart: always
    build:
      context: .
      dockerfile: ./Dockerfile
    pull_policy: build
    # volumes:
    #   - type: bind
    #     source: ./docker-compose.yml
    #     target: /app/docker-compose2.yml
    #     read_only: true
    volumes:
      # - ./docker-compose.yml:/app/docker-compose2.yml
      - ./test-volumes:/app/test-volumes2
    # ports:
    #  - "8079:80"
    environment:
     - NGINX_HOST=foobar.com
     - NGINX_PORT=80
